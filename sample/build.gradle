apply plugin: 'com.android.application'

android {
    compileSdkVersion 22
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "com.buzzvil.buzzscreen.sample"
        minSdkVersion 11
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }

    productFlavors {
        basic {}
        custom {}
        multiProcess {}
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.google.android.gms:play-services-ads:9.4.0'
    compile 'com.nostra13.universalimageloader:universal-image-loader:1.9.4'

    basicCompile(name:'buzzscreen-sdk-full-1.2.6', ext:'aar')
    customCompile(name:'buzzscreen-sdk-core-1.2.6', ext:'aar')
    multiProcessCompile(name:'buzzscreen-sdk-full-1.2.6', ext:'aar')
}

repositories{
    flatDir{
        dirs 'libs'
    }
}
