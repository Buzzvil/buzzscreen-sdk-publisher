<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.buzzvil.buzzscreen.sample">
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Configuration for BuzzScreen -->
        <meta-data
            android:name="app_license"
            android:value="<app_license>" />
        <meta-data
            android:name="com.buzzvil.locker.mediation.baidu.plist"
            android:value="<plist>" />
        
        <!--optional: Mobvista 연동 파트너사의 경우 아래의 메타데이터 추가 필요 (BuzzScreen SDK 1.8.5.1 이상)-->
        <meta-data
            android:name="com.buzzvil.locker.mediation.mobvista.app_id"
            android:value="<app_id>" />

        <meta-data
            android:name="com.buzzvil.locker.mediation.mobvista.app_key"
            android:value="<app_key>" />
        
        <!-- Optional: Opt-in mopub mediation.
        <meta-data
            android:name="com.buzzvil.locker.mediation.mopub.ad_unit_id"
            android:value="<ad_unit_id>" />
        -->
    </application>
</manifest>
